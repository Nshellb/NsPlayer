<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="12dp"
    android:paddingBottom="4dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/inquiry_subject"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body2" />

    <EditText
        android:id="@+id/inquirySubjectInput"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/bg_inquiry_input"
        android:hint="@string/inquiry_subject_hint"
        android:inputType="textCapSentences"
        android:maxLines="1"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:textCursorDrawable="@drawable/cursor_brand_green" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/inquiry_message"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body2" />

    <EditText
        android:id="@+id/inquiryMessageInput"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/bg_inquiry_input"
        android:gravity="top|start"
        android:hint="@string/inquiry_message_hint"
        android:inputType="textCapSentences|textMultiLine"
        android:overScrollMode="ifContentScrolls"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:scrollHorizontally="false"
        android:scrollbars="vertical"
        android:textCursorDrawable="@drawable/cursor_brand_green" />

</LinearLayout>
