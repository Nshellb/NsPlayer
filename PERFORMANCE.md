# 성능 개선 및 확인 항목

## 적용한 변경

- 릴리스에 R8 코드 최적화·난독화와 리소스 축소를 활성화했다. 기존 AGP 9.0.0과 라이브러리 버전은 유지한다.
- 영상을 열기 전 화면 방향 확인용 `MediaMetadataRetriever` 호출을 제거하고 플레이어의 `VideoSize` 정보를 사용한다.
- 외부 파일명 조회와 자막 인코딩 변환을 작업 스레드로 옮겼다. 자막 변환은 스트림으로 처리하고 변환 중에도 영상 준비를 진행한다.
- 자막 변경 시 현재 미디어 항목만 교체한다. 자동 자막 탐색은 현재 영상과 인접 영상부터 수행한다.
- 폴더 목록의 불필요한 자막 조회를 생략하고, 영상 목록의 자막 조회는 저장소별 최대 100개 경로를 묶어 처리한다. 검색 미리보기의 개수 제한도 자막 조회 전에 적용한다.
- 목록을 캐시 파일 저장 전에 표시하며, 오래된 검색 결과는 입력 변경 시 무효화한다. 재활용된 행의 썸네일 요청을 해제한다.
- 시작·복귀 시 필요한 설정만 읽고, 설정이 같으면 중복 알림을 생략한다.
- 최근 기록 저장과 위젯 갱신은 앱 공용 작업 큐에서 처리한다. 위젯은 문구와 목록 데이터만 갱신한다.

## 검증 범위

프로젝트 지침에 따라 이 작업에서는 빌드 및 빌드 테스트를 실행하지 않았다. 코드 흐름, 호출부, 변경 파일의 공백 오류를 정적으로 검토했다. 실제 시작 시간, 메모리 감소량, 릴리스 실행 및 Play Console 점수는 아직 측정·확인하지 않았다.

최근 기록 작업 큐는 호출 순서대로 저장하지만 프로세스가 작업 실행 전에 강제 종료되면 마지막 기록이 반영되지 않을 수 있다. 영상 이어보기 위치는 기존 플레이어의 별도 저장 경로를 유지한다.

## 이후 실기기 확인

사용자가 빌드·테스트를 요청하면 다음을 확인한다.

1. 같은 기기·파일로 앱 최초 실행과 재실행을 나눠 목록에서 선택한 시점부터 첫 프레임까지 비교한다. 내부 저장소, SD 카드, 큰 영상, 회전 정보가 있는 영상을 포함한다.
2. UTF-8, EUC-KR, CP949 자막을 자동·수동으로 선택하고, 변환 중 다른 영상 열기·자막 끄기·앱 복귀를 확인한다. 재생 위치와 일시정지 상태가 유지되는지도 확인한다.
3. 연속 재생·셔플·반복, 화면 회전, PiP 진입·복귀, 이어보기 및 최근 기록 위젯을 확인한다.
4. 영상이 많은 폴더·계층 목록·검색에서 정렬, 자막 표시, 빠른 검색어 수정·삭제, 캐시 표시 이후 갱신을 확인한다.
5. 최적화된 릴리스에서 재생·설정·위젯을 확인하고, 해당 배포 파일을 Play Console에 업로드한 뒤 R8 메타데이터와 최적화 지표를 다시 확인한다. 기존 업로드의 지표는 소스 설정 변경만으로 갱신되지 않는다.

R8 구성은 [Android 공식 가이드](https://developer.android.com/topic/performance/app-optimization/enable-app-optimization)의 AGP 9.0 설정을 따른다. 새 릴리스의 오류 분석에는 해당 빌드의 `app/build/outputs/mapping/release/mapping.txt`를 보관한다.
